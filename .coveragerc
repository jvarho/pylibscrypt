[report]
exclude_lines =
    # Not called from coverage tests
    if __name__ == .__main__.:

    # Untestable from a working environment
    except AttributeError:
    except OSError:
    except TypeError
    raise ImportError.*

    # py-scrypt API not used (encrypt and decrypt)
    maxmemfrac=MAXMEMFRAC_DEFAULT\):
    encoding='utf-8'\):

