[report]
exclude_lines =
    # Not called from coverage tests
    if __name__ == .__main__.:

    # Untestable from a working environment
    except AttributeError:
    except OSError:
    raise ImportError.*

    # Duh...
    def run_tests

